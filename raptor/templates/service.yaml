apiVersion: v1
kind: Service
metadata:
  name: {{ include "raptor.fullname" . }}
spec:
  selector:
    {{- toYaml .Values.labels | nindent 4 }}
  type: NodePort
  ports:
  - port: 8089
    targetPort: 8089
    nodePort: 30002
